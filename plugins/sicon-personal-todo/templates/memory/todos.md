# Personal todos

Load when triggered only (`/todo-list`, `/todo-explain`, `/todo-add`, `/todo-resume`, `todo-triggers` rule). Flow: [`memory.md`](memory.md) § Session resume.

Item format when adding: [`todos/FORMAT.md`](todos/FORMAT.md) — agents must pass § Quality bar (`todo-add` pushes back if sparse).

## Active

*Order: non-blocked first (by id), then blocked (by id).*

## Done

*Order: by id.*

## Parked

*Order: by id / epic title.*
